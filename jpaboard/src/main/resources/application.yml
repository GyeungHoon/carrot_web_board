server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://primedb.sldb.iwinv.net/MARKETING_BD?useUnicode=true&characterEncoding=utf8&autoReconnect=true&serverTimezone=Asia/Seoul&useOldAliasMetadataBehavior=true&zeroDateTimeBehavior=convertToNull
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: admin_carrot
    password: admin_carrot1!
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        use_sql_comments: true
  thymeleaf:
    cache: false  # ✅ 올바른 위치로 이동

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace
